import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    root: {
        background: "red",
        display: "flex",
        justifyContent: "space-evenly",
        gap: "10",
        width: "100%"
    }
}))
